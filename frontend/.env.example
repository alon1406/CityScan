# Backend API base URL (no trailing slash)
VITE_API_URL=http://localhost:5000
# Demo mode: when 'true', login page shows only the two big Demo buttons (User + Admin). Also used for offline demo.
# VITE_IS_DEMO=true
# AI service for demo photo analysis (optional; e.g. http://localhost:8001)
# VITE_AI_SERVICE_URL=http://localhost:8001
